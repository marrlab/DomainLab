"""
for testing one needs a network, which is actually not a network
"""


def build_feat_extract_net(dim_y, remove_last_layer):
    """
    I am not a neuronal network
    """
    # I had to add these two prints to satisfy the requirements of codacy
    # to use all arguments given to a function
    print(str(remove_last_layer))
    print(str(dim_y))
    return None
